import { AssetFinderOptions, PlayerDataAPI } from "../../types";
import { Characters } from "./Characters";
import { HoyoBuilds, Hoyos, Owner } from "../Enka";
import { Player } from "./Player";
/**
 * A class that structures the data.
 */
export declare class PlayerData {
    private language;
    /**
     * The player's information.
     */
    player: Player;
    /**
     * The player's showcase characters information.
     */
    characters: Characters[];
    /**
     * Time in seconds until the profile data get refreshed.
     */
    ttl: number;
    /**
     * The Enka profile data that belongs to the UID.
     */
    owner: Owner;
    /**
     * The player's UID.
     */
    uid: string;
    /**
     * The Request Handler.
     */
    private readonly handler;
    /**
     * Creates a new `PlayerData` instance.
     * @param data - The data of the API.
     * @param language - The language to get the names.
     */
    constructor(data: PlayerDataAPI, language: AssetFinderOptions["language"]);
    getHoyo(): Promise<Hoyos>;
    getHoyoBuilds(): Promise<HoyoBuilds[]>;
}
